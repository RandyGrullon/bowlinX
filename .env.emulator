# pnpm dev:emu -> Auth y Firestore contra los emuladores locales (no tocan el proyecto real).
# El escaneo con IA sí usa el proyecto real (Firebase AI Logic no tiene emulador).
VITE_USE_EMULATORS=true
